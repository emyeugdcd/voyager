{{- if .Values.config.apiUrl }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-config
  labels:
    app: {{ include "frontend.fullname" . }}
data:
  config.js: |
    window.REACT_APP_API_URL = "{{ .Values.config.apiUrl }}";
{{- end }}
