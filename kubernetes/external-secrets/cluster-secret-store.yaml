apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: azure-keyvault
spec:
  provider:
    azureKV:
      authType: WorkloadIdentity
      vaultUrl: "https://voyager-test-kv-60d48676.vault.azure.net/"
      serviceAccountRef:
        name: external-secrets-sa
        namespace: external-secrets
